from flask import request

@app.route(f"/game/{}")
def game_{}() -> None:
    ip_address = request.access_route[-1]
    
    if {} not in games_dictionary:
        return render_template("no_game_with_such_id.html")

    if games_dictionary[{}] is None:
        return render_template("no_game_with_such_id.html")

    if games_dictionary[{}]["player_2"] is None:
        if ip_address != games_dictionary[{}]["player_1"]:
            games_dictionary[{}]["player_2"] = ip_address

    return render_template("game.html")